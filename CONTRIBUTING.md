# 공동 연구 작성 규칙

## 1. 작업 시작

1. 대시보드에서 현재 단계와 담당자를 확인합니다.
2. 해당 GitHub Issue를 열어 작업 내용을 확인합니다.
3. 담당자가 없으면 자신을 담당자로 지정합니다.
4. 개인 폴더에 날짜별 작업 기록을 만듭니다.

## 2. 파일 저장 위치

- 개인 작업 중인 코드와 메모: `members/<사용자명>/`
- 검토가 끝난 기준 코드와 결과: `shared/`
- 대용량 TDR·PLT·DAT·LOG: 연구실 서버
- 대용량 파일의 위치와 설명: `data_manifest/server_data_paths.csv`

## 3. 파일 이름

작성자 이름 대신 작업과 조건이 드러나도록 작성합니다.

좋은 예:

- `P1_D0_Vd070_interface_profile.csv`
- `P2_SWF_VCAT_IdVg.png`
- `P4_Write1_Hold1_summary.md`

피해야 할 예:

- `상현최종.cmd`
- `수정본2.cmd`
- `진짜최종_final.cmd`

## 4. 작업 완료

다음 항목이 모두 준비된 뒤 Issue를 완료합니다.

- 실행 가능한 코드
- 필요한 그래프와 표
- 사용 조건
- 결과 해석
- 원본 파일 위치
- 오류와 재실행 여부
- 다음 작업자 인수인계
- 검토자 확인

## 5. 공용 파일 반영

개인 폴더의 파일을 바로 기준 파일로 사용하지 않습니다. 검토가 끝난 자료만 `shared/`에 옮깁니다.

## 6. 대용량 파일

GitHub에는 재현에 필요한 코드, 요약 CSV, 그래프, 설명 문서를 올립니다. 원본 시뮬레이션 데이터는 서버에 두고 경로를 기록합니다.
